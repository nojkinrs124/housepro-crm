/**
 * Рубрики базы знаний — подсказки для поля «Рубрика», само поле свободное.
 *
 * Лежит отдельно от actions намеренно: в файле с 'use server' разрешены
 * только async-экспорты, константа оттуда роняет весь роут в рантайме.
 */
export const KNOWLEDGE_CATEGORIES = [
  'Общее', 'Продажи', 'База объектов', 'Документы', 'Деньги', 'Администрирование',
] as const
