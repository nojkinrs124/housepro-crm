# Скрытое, но не удалённое

Реестр функций, убранных с экрана в ходе упрощения интерфейса (`/simplify-ux`).
Код и данные на месте. Через 2–3 недели после выката — второй проход: что за это
время не понадобилось, удаляется; что понадобилось — возвращается на экран.

Правило записи: дата · что скрыто · где был код · почему · как вернуть.

| Дата | Что скрыто | Код | Почему | Как вернуть |
|---|---|---|---|---|
| 2026-09-16 | Комментарии к сделке (блок на карточке) | `src/features/deals/components/DealComments.tsx`, экшены `deal-comments.actions.ts` | 0 записей на проде, один сотрудник в системе — комментировать некому | Вернуть `<DealComments>` в `deals/[id]/page.tsx` и запрос `deal_comments` в `Promise.all` |
| 2026-09-16 | ~~Вложения (файлы) на карточке сделки~~ — **возвращено 17.09.2026** (`<FilesSection dealId>`, бакет `documents` создан, #35 закрыта) | `src/features/files/components/FilesSection.tsx` | — | — |
| 2026-09-16 | Лента коммуникаций на карточке сделки | `src/features/communications/components/CommunicationTimeline.tsx` | 0 записей, телефония/мессенджеры не подключены (решение B2.3) | Вернуть `<CommunicationTimeline>` в правую колонку карточки |
| 2026-09-16 | Блок «Быстрые действия» на карточке сделки | — (удалён как дубль) | Дублировал «Задача» в блоке задач и «Карточка объекта» в блоке объекта (правило «одно действие — один способ») | Не возвращать; действия есть в своих блоках |
| 2026-09-16 | Лента общения на карточке контакта | `CommunicationTimeline` | 0 коммуникаций, каналы не подключены (B2.3) | Вернуть `<CommunicationTimeline contactId phone>` в правую колонку `contacts/[id]/page.tsx` |
| 2026-09-16 | Панель «Создать сделку / Создать договор» внизу карточки контакта | — (удалена как дубль) | «Новая сделка» — главная кнопка шапки, «Создать договор» — в меню «…» | Не возвращать |
| 2026-09-16 | Счётчики и показания на карточке объекта (`MetersPanel`) | `src/features/properties/components/MetersPanel.tsx` | 0 счётчиков; блок остаётся на карточке объекта в управлении (`management/[id]`) | Вернуть `<MetersPanel propertyId meters>` и запрос `utility_meters` в `properties/[id]/page.tsx` |
| 2026-09-16 | ~~Вложения (`FilesSection`) на карточке объекта~~ — **возвращено 17.09.2026** (#35 закрыта) | `src/features/files/components/FilesSection.tsx` | — | — |
| 2026-09-16 | Панель «Создать сделку / Создать договор» внизу карточки объекта | — (удалена как дубль) | «Новая сделка» — главная кнопка шапки, «Договор» — в блоке договоров | Не возвращать |
| 2026-09-16 | Панели электронного подписания на карточке договора (внутренняя ПЭП и Подпислон) | `ContractSigningPanel.tsx`, `PodpislonSigningPanel.tsx`, экшены `contract-signing.actions.ts`, `podpislon.actions.ts` | 0 подписей на проде (решение B2.2) | Вернуть оба компонента и запросы `contract_signatures`/`channel_integrations` в `contracts/[id]/page.tsx` |
| 2026-09-16 | Кнопка «Скопировать договор» (`/contracts/new?duplicate=`) | — (удалена) | Параметр `duplicate` форма никогда не читала — кнопка ничего не копировала | Если нужно копирование — реализовать чтение параметра в `contracts/new` |
| 2026-09-16 | Кнопки «Сверка с банком», «Периодические», «Категории», «CSV» в шапке бухгалтерии | — (перенесены в меню «…») | 0 правил повторения; одна главная кнопка на экран (B3.3) | Уже доступны в меню «…»; вернуть в шапку — вынести из `more` в `secondary` в `accounting/page.tsx` |
| 2026-09-16 | Лента коммуникаций на карточке лида | `CommunicationTimeline` | 0 коммуникаций (B2.3) | Вернуть `<CommunicationTimeline leadId phone>` в правую колонку `leads/[id]/page.tsx` |
| 2026-09-16 | «Создать сделку» с карточки лида | — (удалена) | Передавала id лида как `client_id`, форма сделки молча ничего не подставляла (C7.2); путь — «В контакты» → «Новая сделка» | Не возвращать |
| 2026-09-16 | Настройки → «Каналы связи» (`/settings/channels`) и «Электронная подпись» (`/settings/signing`) | страницы на месте, убраны из хаба настроек | 0 коммуникаций, 0 подписей (B2.2, B2.3) | Вернуть пункты в `SETTINGS_GROUPS` в `settings/page.tsx` |
| 2026-09-16 | Настройки → API-ключи и Вебхуки | в свёрнутом блоке «Для разработчиков», только admin | 1 ключ, 0 вебхуков (B2.1) | Перенести из `DEVELOPER_ITEMS` в группу «Интеграции» |
| 2026-09-16 | Пункты сайдбара «Подборки», «Заявки», «Календарь», «База знаний» | `Sidebar.tsx` (navigation) | 0 подборок и заявок (B1.1, B1.2); календарь — кнопка в «Задачах» (B1.4); база знаний — «?» в шапке (B1.5) | Вернуть строки в массив `navigation` |
| 2026-09-16 | Пункт «Сотрудники» для ролей кроме admin | `Sidebar.tsx` (`adminOnly`) | 1 сотрудник (B1.6) | Убрать `adminOnly: true` |
| 2026-09-17 | Telegram-бот: раздел меню «🔎 Анализ рынка» (экран `multiagent`, кнопка `magent:research`) | — (удалён как дубль); `src/lib/telegram/market-research.ts` на месте, вызывается инструментом `market_research` из диалога | Два входа в одно действие: тот же веб-поиск есть в диалоге («сравни с рынком») | Не возвращать; спрашивать в чате |
| 2026-09-17 | Telegram-бот: команды `/post`, `/case`, `/pause`, `/resume` | — (обработчики удалены, старая команда отвечает подсказкой «в /menu → 📢 Канал») | Дублировали кнопки меню «📢 Канал» и «⚙️ Настройки» | Не возвращать |
| 2026-09-17 | Telegram-бот: инструменты модели `create_contact`, `update_contact`, `import_client_request`, `import_property_extract`, `import_rental_contract`, `generate_contract`, `check_contract` | API-роуты `/api/v1/import/*` и `/api/v1/contacts` живы (публичный API); Postgres-функции — обёртки над `import_intake` | Документы заводит кнопочный сценарий «📥 Занести в CRM», где тип задаёт пользователь; `generate/check_contract` требовали UUID договора, который модели неоткуда взять | Вернуть определения в `TOOL_DEFINITIONS` и `MUTATING_TOOLS` (`src/lib/telegram/tools.ts`) |
